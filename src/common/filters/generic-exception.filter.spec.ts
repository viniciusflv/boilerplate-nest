import { GenericExceptionFilter } from './generic-exception.filter';

describe('GenericExceptionFilter', () => {
  it('should be defined', () => {
    expect(new GenericExceptionFilter()).toBeDefined();
  });
});
